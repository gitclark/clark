<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="david.mybatis.dao.VisitorDao">

	<resultMap type="Visitor" id="visitorRs">
        <id column="id" property="id" />
        <result column="Name" property="name" />
        <result column="Email" property="email" />
        <result column="Status" property="status" />
        <result column="CreateTime" property="createTime" />
    </resultMap>
    
<!-- 此处的resultType就是对应刚刚你在typeAlias节点里面规定的别名 -->
    <select id="sayHello" parameterType="int" resultType="Visitor">
        select * from visitor where id=#{id} and Status>0 order by Id
    </select>
    
    <select id="selectAll" resultType="Visitor">
    	select * from visitor
    </select>
    
    <insert id="add" parameterType="Visitor">
    	<selectKey keyProperty="id" resultType="int" order="BEFORE">
    		select VISITOR_SEQ.nextVal from dual
    	</selectKey>
    	insert into visitor(id,name,email,status,createtime) values(#{id},#{name},#{email},#{status},#{createTime,jdbcType=TIMESTAMP})
    </insert>
    
    <update id="updateVis" parameterType="Visitor">
    	update visitor 
    	<set>
    	<if test="name!=null">
    		name=#{name},
    	</if>
    	<if test="status!=null">
    		status=#{status},
    	</if>
    	<if test="createTime!=null">
    		createtime=#{createTime,jdbcType=TIMESTAMP}
    	</if>
    	 	where id=#{id}
    	</set> 
    </update>
    
    <delete id="delete" parameterType="int">
    	delete from visitor where id=#{id}
    </delete>
</mapper>